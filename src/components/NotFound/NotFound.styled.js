import { styled } from "styled-components";

export const NotFoundStyle = styled.li`
    text-align: center;
    list-style-type: none;
    margin-top: 25px;

    .not__found{
        color: #000;
        font-family: Georgia, 'Times New Roman', Times, serif;
        font-size: 45px;
    }
`